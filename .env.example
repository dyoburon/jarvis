# Jarvis — Environment Configuration
# Copy to .env and fill in what you have. All values are optional.

# Google Gemini — enables voice routing and data skills
# Get a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# Projects directory — where Jarvis looks for code projects
# Defaults to the jarvis repo root if not set
PROJECTS_DIR=

# Claude Code OAuth token — only needed if using the proxy connector
# Most setups use CLI auth instead (claude auth login)
CLAUDE_CODE_OAUTH_TOKEN=
