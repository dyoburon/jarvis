# Jarvis environment config
# Copy to .env and fill in what you have.
# All values are optional — the app degrades gracefully.

# Gemini (enables voice routing + data skills)
GOOGLE_API_KEY=

# Presence server
PRESENCE_URL=ws://localhost:8790
